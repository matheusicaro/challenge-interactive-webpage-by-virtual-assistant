describe("Test 'typedefs.ts'", () => {
  describe("Test Mutations 'Message'", () => {
    describe("Check 'sendMessage'", () => {
      test('Should validate input and expected returns as per the integration contract.', () => {
        /**
         * TODO: missing test due to lack of developer time
         */
      });
    });
  });

  describe("Check type 'Message'", () => {
    test('Should return type Message with expected attributes to keep the integration contract', () => {
      /**
       * TODO: missing test due to lack of developer time
       */
    });
  });

  describe("Check type 'MessageContext'", () => {
    test('Should return type Command with expected attributes to keep the integration contract', () => {
      /**
       * TODO: missing test due to lack of developer time
       */
    });
  });

  describe("Check type 'Command'", () => {
    test('Should return type Command with expected attributes to keep the integration contract', () => {
      /**
       * TODO: missing test due to lack of developer time
       */
    });
  });

  describe("Check enum 'CommandType'", () => {
    test('Should return return expected enums.', () => {
      /**
       * TODO: missing test due to lack of developer time
       */
    });

    test('Should return return expected order of enums.', () => {
      /**
       * TODO: missing test due to lack of developer time
       */
    });
  });
});
